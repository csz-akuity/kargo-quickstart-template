---
# Warehouse for kargo-quickstart-template - Team Beta specific
apiVersion: kargo.akuity.io/v1alpha1
kind: Warehouse
metadata:
  name: kargo-quickstart-template-team-beta
  namespace: kargo-quickstart-template-team-beta
  labels:
    team: beta
spec:
  subscriptions:
  # Watch container images 
  - image:
      repoURL: ghcr.io/csz-akuity/guestbook
      semverConstraint: ^0.0
      discoveryLimit: 5
      
  # Watch Git repo for config changes
  - git:
      repoURL: https://github.com/csz-ghorg/kargo-quickstart-template.git
      branch: team-beta-dev